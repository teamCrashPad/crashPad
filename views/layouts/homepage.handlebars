<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="Content-type" content="text/html; charset=utf-8">

    <!-- Latest compiled and minified CSS & JS -->
    <link rel="stylesheet" media="screen" href="/assets/bootstrap/css/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery.js"></script>
    <script src="/assets/bootstrap/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="/assets/css/style.css">

    <title>Find Student Affordable Housing Today</title>

    <style>
        #hero_img {
            background-image: url(/assets/Images/hero_bkg.jpg);
            opacity: .9;
            height: 700px;
            width: 100%;
            background-position: center;
            background-size: cover;
        }

        .left-triangle {
            width: 0px;
            height: 0px;
            border-top: 0px solid transparent;
            border-bottom: 100px solid transparent;
            border-left: 300px solid #FFD266;
            border-right: 0px solid transparent;
            position: relative;
            top: -19px;
        }

        .triangle-text {
            padding-left: 15px;
            padding-top: 15px;
            font-size: 15px;
            letter-spacing: .75px;
        }

        .triangle-text a {
            color: black;
        }

        .hero_content {
            position: absolute;
            top: 155px;
            width: 100%;
            background: #fff4;
            padding: 20px;
        }

        #hero_img2 {
            width: 75%;
            margin: 0 auto;
            text-align: center;
            opacity: 1;
        }

        .hero-form {
            width: 75%;
            margin: 30px auto;
        }

        #hero-input {
            height: 50px;
            text-transform: capitalize;
            letter-spacing: .5px;
            font-size: 20px;
            color: black;
            font-weight: 800;
        }

        .hero-input-search {
            color: white;
            background-color: #FFD266;
            border: none!important;
            padding: 0 20px;
            cursor: pointer;
        }
    </style>
</head>

<body>
    {{{body}}}
</body>

</html>